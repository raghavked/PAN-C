export { colors } from './colors';
export { typography } from './typography';
export { spacing, spacingNum, borderRadius, elevation } from './spacing';

import { colors } from './colors';
import { typography } from './typography';
import { spacing, spacingNum, borderRadius, elevation } from './spacing';

export const theme = {
  colors,
  typography,
  spacing,
  spacingNum,
  borderRadius,
  elevation,
};

export default theme;
